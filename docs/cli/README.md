# 노안중 AI — CLI 작업 묶음 (2026-09-06)

**연수가 내일 9/7(월) 13:00이다. 우선순위를 지켜라.**

이전에 지시서만 채팅으로 보내고 입력 파일을 저장소에 안 넣어서 한 번 헛돌았다.
이번엔 `inputs/` 에 필요한 파일을 전부 넣어 뒀다. **거기서 복사해서 쓴다.**

---

## A. 오늘 안에 — 이것만 하면 내일이 산다

### A-1. `guide/` 웹판 (지시서: `instructions/3f-guide.md`)

내일 QR로 안내서를 띄운다. **이게 없으면 QR이 죽은 링크가 된다.**
지금 QR은 `zln02.github.io/noan-ai/guide/` 를 가리키는데 그 쪽이 아직 없다.

넣을 파일:

| inputs 안 경로 | 저장소 경로 |
|---|---|
| `inputs/guide/tasks.json` | `guide/tasks.json` |
| `inputs/guide/노안중_AI안내서.pdf` | `guide/노안중_AI안내서.pdf` |
| `inputs/guide/build2.py` | `tools/guide-build.py` (참고용) |
| `inputs/guide/qr-guide.svg` | `assets/qr-guide.svg` |
| `inputs/guide/qr-request.svg` | `assets/qr-request.svg` |

`tasks.json` 은 **3(f-1)까지 반영된 최종본**이다. 3(f) 지시서에 딸려 있던 옛 버전이 아니다.
`meta.tip` 필드가 새로 있고, 길찾기에서 `▶` 가 빠져 있다 — `instructions/3f1-width.md` 참고.

**`tasks.json` 의 문장을 한 글자도 고치지 마라.** 인쇄본 PDF와 대조 검증한다.

### A-2. `library/` 문구 정정 (같은 지시서 4절)

| 지금 | 고칠 것 |
|---|---|
| Pro 반영 "10~20분" | **"보통 2~5분"** |
| 인증 "간편인증 **또는** 공동인증서" | **"간편인증과 공동인증서를 둘 다 등록"** |

두 번째가 중요하다. "또는" 으로 안내하면 절반이 메뉴 이동에서 막힌다.

### A-3. 검증 — 이것만은 꼭

1. `guide/` 가 실제로 열리는지 (`file://` 와 배포본 양쪽)
2. 카드 16장 · 업무 10 / 수업 6
3. 프롬프트 **복사 버튼**이 실제로 동작하는지
4. `grep -rn "10~20분\|간편인증 또는" .` → **0건**
5. `grep -rn "도구모음 ▶" .` → **0건**
6. 외부 요청 0건 · `private/` 0건

**A가 끝나면 진영님께 배포 주소를 보고하고 멈춰라.** 그 다음이 A-4다.

### A-4. 덱 레이아웃 교체 (지시서: `instructions/3j-template.md`, 입력: `inputs/template/템플릿 1.pdf`)

**A-1~A-3이 끝난 뒤에만 시작한다.** 안내서가 먼저다.

- 원고·순서·id·data-t 불변. CSS와 class만.
- **색은 안 바꾼다.** 이유는 지시서 0절.
- 템플릿 그림 파일은 저장소에 넣지 않는다 (라이선스).
- 노아리 3자리는 `inputs/noari/` → `assets/noari/` 로 먼저 복사.
- **24장 중 하나라도 1280×720을 넘치면 커밋하지 마라.** 내일 화면에 잘려 나온다.
- 밤 늦게까지 안 끝나면 멈추고 보고해라. 지금 덱도 쓸 수 있다.

---

## B. 연수 뒤에 — 급하지 않다

### B-1. 마스코트 노아리 (지시서: `instructions/3i-noari.md`)

`inputs/noari/` 의 7종을 `assets/noari/` 로. 배치 11곳 + 파비콘 + 떠오르는 동작.

- 내가 예전에 그린 `noa.svg` · `modu.svg` · `mascot.svg` 는 **버린다.** 저장소에 있으면 지워라.
- 덱 s2·s24 의 `teacher-hello.svg` · `pair-tablet.svg` 도 노아리로 교체한다.
- **`grep -rn "늙은 기러기" .` → 0건.** 노안면은 `老安面` 이고 기러기 `雁` 이 아니다. 이유는 지시서 3절.

### B-2. AI 지도 아이콘

simple-icons 에 실제로 있는 것(`openai` `canva` `adobe` `capcut` `microsoftcopilot`)은 진짜 로고로 바꿔라.
국내 도구(뤼튼·클로바·띵커벨·패들렛·엔트리 등)는 지금의 텍스트 뱃지를 유지하되 스타일을 통일해라.

### B-3. 가드

`tools/gen-map.py` 상단에 가드 주석을 달고, `--force` 없이는 기존 파일을 덮어쓰지 못하게 해라.

---

## 하지 말 것 (전 구간 공통)

- `tasks.json` · 덱 원고 문장 다듬기 — **연수 대본이다**
- `data-t` 건드리기 (덱 24장 그대로)
- `tokens.css` 에 없는 새 색 만들기
- 외부 라이브러리 추가 (검색·복사·라이트박스 전부 직접 짜라)
- `private/` 커밋
- 노션 토큰·API 키를 저장소나 브라우저에 넣기

## 커밋

```
fix: 미리캔버스 Pro 반영 시간과 인증 절차를 교육청 매뉴얼 기준으로 정정
feat: 업무별 AI 안내서 16가지 — 카드 진입, 프롬프트 복사
fix: 도구모음 안내를 화면 너비와 무관하게 정정
feat: 마스코트 노아리 — 7종 배치, 파비콘, 떠오르는 동작
```

## 보고할 것

1. `guide/` 배포 주소와 카드 수
2. 복사 버튼 동작 (성공 경로 · `file://` 실패 경로 둘 다)
3. `grep` 두 개 결과
4. 인쇄본 PDF와 웹 문장 대조 결과
5. 커밋 해시
